'use strict'

// app.use(authentication);

const jwt = require('jsonwebtoken');
const Token = require('../models/token');

module.exports = async (req, res, next) => {

    


    next();
};

//! auth system will be built